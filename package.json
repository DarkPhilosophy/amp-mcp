{
  "name": "amp-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server that runs Amp CLI as a subagent",
  "main": "index.js",
  "scripts": {
    "lint": "node --check index.js",
    "test": "node --check index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0"
  }
}
